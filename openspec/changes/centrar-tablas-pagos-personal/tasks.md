## 1. Centrar tablas en pagos_personal.html

- [x] 1.1 Agregar clase `tables--single` al `<section class="tables">` en `pagos_personal.html`
- [x] 1.2 Agregar regla CSS `.tables--single { grid-template-columns: minmax(0, 1fr); }` en `panel.css` para forzar columna única
- [x] 1.3 Agregar `justify-content: center` a `.view-toggle` en `panel.css` para centrar los botones

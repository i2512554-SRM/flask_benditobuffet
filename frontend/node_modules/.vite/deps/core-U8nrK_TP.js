import { b as fe } from "./object-EGs2rUUd.js";
import { U as computed, nt as defineComponent, pt as h } from "./vue.runtime.esm-bundler-D4M2X7UN.js";
//#region node_modules/@primeicons/vue/dist/esm/core/index.mjs
var d = ([t, o]) => {
	const { key: i, ...e } = o, n = {};
	for (const [s, r] of Object.entries(e)) n[fe(s)] = r;
	return h(t, {
		key: i,
		...n
	});
};
var v = (t) => {
	const o = {
		size: {
			type: [Number, String],
			default: void 0
		},
		color: {
			type: String,
			default: void 0
		},
		spin: {
			type: Boolean,
			default: !1
		}
	};
	return {
		Icon: defineComponent({
			name: t.name.split("-").map((e) => e.charAt(0).toUpperCase() + e.slice(1)).join(""),
			props: o,
			setup(e, { attrs: n }) {
				const s = computed(() => e.size ?? 20), r = computed(() => ({
					...e.size && { "--px-icon-size": `${e.size}px` },
					...e.color && { color: e.color }
				})), a = computed(() => [
					"p-icon",
					`p-icon-${t.name}`,
					e.spin && "p-icon-spin"
				].filter(Boolean));
				return () => h("svg", {
					...t.svg,
					width: s.value,
					height: s.value,
					"aria-hidden": "true",
					...n,
					style: r.value,
					class: a.value
				}, t.nodes.map(d));
			}
		}),
		props: o
	};
};
//#endregion
export { v as t };
